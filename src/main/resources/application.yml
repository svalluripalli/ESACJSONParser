server:
  port: 8080
    
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    org.springframework.web: TRACE
    org.apache.http: TRACE
    org.springframework.security: TRACE
  file: /var/tmp/educom/logs/ca.log